apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-produtos-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-produtos
  template:
    metadata:
      labels:
        app: api-produtos
    spec:
      containers:
      - name: api-produtos
        image: mamazzei/api-produtos
        resources:
          limits:
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: api-produtos-service
spec:
  type: NodePort
  selector:
    app: api-produtos
  ports:
    - protocol: TCP     
      port: 5000
      targetPort: 5000
